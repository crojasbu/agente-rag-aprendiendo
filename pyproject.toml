[tool.poetry]
name = "seccion-1"
version = "0.1.0"
description = ""
authors = ["crojasb"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.13"
langchain = "^0.2.0"                # Versión estable con AgentExecutor
langchain-community = "^0.2.0"
langchain-openai = "^0.1.0"
langchainhub = "^0.1.15"
faiss-cpu = "^1.8.0"
python-dotenv = "^1.0.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
